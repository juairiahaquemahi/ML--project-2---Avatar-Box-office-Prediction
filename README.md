# ML-project-2-Avatar-Box-office-Prediction

I initally chose only XGBOOST to predict boxoffice collection so that it could capture blockbuster performance. The another model is simpler, but includes ensembling of neural network model and XGBoost to reduce overfitting. 
